[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "roadmap-cli"
version = "1.0.0"
description = "A terminal-only curated map of resources for learning to code"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Roadmap CLI contributors"}]
dependencies = []

[project.scripts]
roadmap = "roadmap.cli:main"
