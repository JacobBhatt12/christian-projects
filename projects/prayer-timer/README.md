# Prayer Timer

A simple Python terminal app for focused prayer time.

## What it does

- Asks how many minutes you want to pray
- Shows a live countdown in the terminal
- Prints an encouraging message when time is up

## Usage

```bash
python3 prayer_timer.py
```

Press `Ctrl+C` to end early.

## Requirements

Python 3 only — uses the standard library (`time`), no extra packages needed.
